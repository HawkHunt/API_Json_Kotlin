package com.example.api_json_kotlin

import android.os.Looper
import androidx.test.ext.junit.runners.AndroidJUnit4
import kotlinx.android.synthetic.main.activity_graph_show.*
import org.junit.After
import org.junit.Assert
import org.junit.Before
import org.junit.Test

import org.junit.Assert.*
import org.junit.runner.RunWith
import java.util.logging.Handler

@RunWith(AndroidJUnit4::class)
class GraphShowTest{

    @Before
    fun setUp() {

    }

    @After
    fun tearDown() {
    }

    @Test
    fun testTheTestFunction(){
        val A = 2
        val B = 4
        assertEquals(A*2, B)
    }




    //TODO test Error Exception test if the error that's thrown is the correct one



    //TODO write test function to test if the input for a function is the expected
    // so write two lists, one that you will feed into the function and one of the expected output
    // put them in the SortForNation and then assert if the results are the same.
    
}